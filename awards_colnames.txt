  `id` REAL,
  `generated_unique_award_id` TEXT,
  `is_fpds` INTEGER,
  `transaction_unique_id` TEXT,
  `data_source` TEXT,
  `type` TEXT,
  `type_description` TEXT,
  `piid` TEXT,
  `parent_award_piid` TEXT,
  `fain` TEXT,
  `uri` TEXT,
  `total_obligation` REAL,
  `base_and_all_options_value` REAL,
  `total_subsidy_cost` REAL,
  `total_outlay` REAL,
  `date_signed` REAL,
  `description` TEXT,
  `period_of_performance_start_date` REAL,
  `period_of_performance_current_end_date` REAL,
  `potential_total_value_of_award` REAL,
  `last_modified_date` REAL,
  `certified_date` REAL,
  `create_date` REAL,
  `update_date` REAL,
  `total_subaward_amount` REAL,
  `subaward_count` INTEGER,
  `awarding_agency_id` INTEGER,
  `funding_agency_id` INTEGER,
  `latest_transaction_id` REAL,
  `parent_award_id` INTEGER,
  `place_of_performance_id` REAL,
  `recipient_id` REAL,
  `category` TEXT,
  `fiscal_year` INTEGER,
  `total_loan_value` REAL,
  `total_funding_amount` REAL,
  `non_federal_funding_amount` REAL